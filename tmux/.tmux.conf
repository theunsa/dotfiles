# Reload config on prefix + r
unbind r
bind r source-file ~/.tmux.conf

unbind C-b
set -g prefix C-a
# Send Ctrl+a to applications by pressing it twice
bind C-a send-prefix

# Allow mouse to resize panes
set -g mouse on

set -g default-terminal "tmux-256color"

# Set move keys between panes
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Plugins

## Tmux Package Manager
set -g @plugin 'tmux-plugins/tpm'

## Plugin for seamless integration between neovim and tmux
set -g @plugin 'christoomey/vim-tmux-navigator'

## Plugin for managing tmux sessions
set -g @plugin 'jimeh/tmuxifier'

#TA: trying out catpuccin for a whil
#
# ## Plugin for tmux theme (dracula)
# set -g @plugin 'dracula/tmux'
# ### Configure dracula theme
# set -g @dracula-show-powerline true
# set -g @dracula-show-location false
# #set -g @dracula-fixed-location "Cape Town"
# #set -g @dracula-plugins "weather"
# set -g @dracula-show-battery-status false
# set -g @dracula-show-flags true
# set -g @dracula-show-left-icon session
# set -g status-position top

## Plugin for tmux theme (catppuccin)
set -g @plugin 'catppuccin/tmux'
### Configure catppuccin theme
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_window_status_style "rounded"
set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application}"
set -agF status-right "#{E:@catppuccin_status_cpu}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
set -agF status-right "#{E:@catppuccin_status_battery}

# Initialize tmux plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
